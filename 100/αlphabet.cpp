#include <bits/stdc++.h>
using namespace std;
#define rep(i, c) for (int i = 0; i < (int)c; i++)
// typedef long long int;

int main()
{
  char s;
  cin >> s;

  if (isupper(s))
  {
    cout << "A" << endl;
  }
  else
  {
    cout << "a" << endl;
  }

  return 0;
}