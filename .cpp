#include <bits/stdc++.h>
using namespace std;
#define rep(i, c) for (int i = 0; i < (int)c; i++)
typedef long long int;

int main()
{
  int N = 0;
  cin >> N;
  vector<int> a(N);
  rep(i, 4) cin >> a[i];
  long ans = LLONG_MAX;
  rep(N, 2)
  {
  }
  cout << ans << endl;
}